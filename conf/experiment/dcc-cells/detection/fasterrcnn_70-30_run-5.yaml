          # @package _global_

defaults:
 - dcc-cells/detection/common

data:
    train: &common
        split_seed: 37
        num_samples:
            - 67  # num of train images - 70% train 30% val
            - 33  # num of validation images - 70% train 30% val
        max_num_train_val_sample: 100
    validation:
        <<: *common   # reinsert 'train' block content

optim:
    batch_size: 1
    batch_accumulation: 3
    log_every: 2