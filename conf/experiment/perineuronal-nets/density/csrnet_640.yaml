# @package _global_

defaults:
 - perineuronal-nets/density/common

data:
    train:
        patch_size: 640
    validation:
        patch_size: 640

optim:
    batch_size: 4
    batch_accumulation: 4