# @package _global_

defaults:
 - perineuronal-nets/detection/common

data:
    train:
        patch_size: 640
    validation:
        patch_size: 640

optim:
    batch_size: 10
    batch_accumulation: 5