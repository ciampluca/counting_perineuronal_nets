# @package _global_

defaults:
 - adi-cells/segmentation/common

data:
    train: &common
        split_seed: 3
        num_samples:
            - 25  # num of train images
            - 25  # num of validation images
    validation:
        <<: *common
