# @package _global_

defaults:
 - adi-cells/detection/common

data:
    train: &common
        split_seed: 47
        num_samples:
          - 50  # num of train images
          - 50  # num of validation images
    validation:
        <<: *common   # reinsert 'train' block content
