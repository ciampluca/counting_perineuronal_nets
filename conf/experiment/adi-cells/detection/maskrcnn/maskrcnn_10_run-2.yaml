# @package _global_

defaults:
 - adi-cells/detection/maskrcnn/common

data:
    train: &common
        split_seed: 67
        num_samples:
          - 10  # num of train images
          - 10  # num of validation images
    validation:
        <<: *common   # reinsert 'train' block content
