# @package _global_

defaults:
 - adi-cells/density/fcrn-a/common

data:
    train: &common
        split_seed: 57
        num_samples:
            - 25   # num of train images
            - 25  # num of validation images
    validation:
        <<: *common
