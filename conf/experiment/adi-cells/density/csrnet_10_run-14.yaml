# @package _global_

defaults:
 - adi-cells/density/common

data:
    train: &common
        split_seed: 5
        num_samples:
            - 10   # num of train images
            - 10  # num of validation images
        target_params:
            method: reflect
    validation:
        <<: *common
