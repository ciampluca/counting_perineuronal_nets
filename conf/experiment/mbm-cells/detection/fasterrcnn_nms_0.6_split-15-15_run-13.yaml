# @package _global_

defaults:
 - mbm-cells/detection/common

model:
    module:
        nms: 0.6

data:
    train: &common
        split_seed: 55
        num_samples:
          - 15  # num of train images
          - 15  # num of validation images
    validation:
        <<: *common   # reinsert 'train' block content
