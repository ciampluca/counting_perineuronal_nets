# @package _global_

defaults:
 - mbm-cells/segmentation/common

data:
    train: &common
        split_seed: 4
        num_samples:
            - 5  # num of train images
            - 5  # num of validation images
    validation:
        <<: *common
