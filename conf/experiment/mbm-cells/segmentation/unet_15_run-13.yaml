# @package _global_

defaults:
 - mbm-cells/segmentation/common

data:
    train: &common
        split_seed: 4
        num_samples:
            - 15  # num of train images
            - 15  # num of validation images
    validation:
        <<: *common
