# @package _global_

defaults:
 - mbm-cells/density/fcrn-a/common

data:
    train: &common
        split_seed: 95
        num_samples:
            - 15   # num of train images
            - 15  # num of validation images
    validation:
        <<: *common
