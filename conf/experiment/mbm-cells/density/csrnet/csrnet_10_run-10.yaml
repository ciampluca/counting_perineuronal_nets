# @package _global_

defaults:
 - mbm-cells/density/common

data:
    train: &common
        split_seed: 1
        num_samples:
            - 10   # num of train images
            - 10  # num of validation images
        target_params:
            method: reflect
    validation:
        <<: *common
