# @package _global_

defaults:
 - vgg-cells/density/common

data:
    train: &common
        num_samples:
            - 50  # num of train images
            - 50  # num of validation images
    validation:
        <<: *common
