# @package _global_

defaults:
 - vgg-cells/density/fcrn-a/common

data:
    train: &common
        split_seed: 17
        num_samples:
            - 16   # num of train images
            - 16  # num of validation images
    validation:
        <<: *common