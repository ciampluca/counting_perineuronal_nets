# @package _global_

defaults:
 - vgg-cells/density/common

data:
    train: &common
        root: "/home/modro/datino/VGG_cells"
        split_seed: 87
        num_samples:
            - 32   # num of train images
            - 68  # num of validation images
    validation:
        <<: *common
