# @package _global_

defaults:
 - vgg-cells/density/common

data:
    train: &common
        num_samples:
            - 32  # num of train images
            - 68  # num of validation images
    validation:
        <<: *common
