# @package _global_

defaults:
 - vgg-cells/density/common

data:
    train:
        num_samples:
            - 64  # num of train images
            - 36  # num of validation images
    validation:
        num_samples: 
            - 64  # num of train images
            - 36  # num of validation images
