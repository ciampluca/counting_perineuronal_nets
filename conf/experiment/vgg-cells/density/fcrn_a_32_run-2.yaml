# @package _global_

defaults:
 - vgg-cells/density/common_fcrn

data:
    train: &common
        split_seed: 67
        num_samples:
            - 32   # num of train images
            - 32  # num of validation images
        target_params:
            method: reflect
    validation:
        <<: *common
