# @package _global_

defaults:
 - vgg-cells/segmentation/common

data:
    train: &common
        split_seed: 37
        num_samples:
            - 16  # num of train images
            - 16  # num of validation images
    validation:
        <<: *common
