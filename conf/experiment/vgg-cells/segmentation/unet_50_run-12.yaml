# @package _global_

defaults:
 - vgg-cells/segmentation/common

data:
    train: &common
        split_seed: 3
        num_samples:
            - 50  # num of train images
            - 50  # num of validation images
    validation:
        <<: *common
