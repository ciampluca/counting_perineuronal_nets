# @package _global_

defaults:
 - vgg-cells/segmentation/common

data:
    train: &common
        split_seed: 67
        num_samples:
            - 32  # num of train images
            - 32  # num of validation images
    validation:
        <<: *common
