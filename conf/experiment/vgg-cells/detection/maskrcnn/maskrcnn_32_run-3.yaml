# @package _global_

defaults:
 - vgg-cells/detection/maskrcnn/common

data:
    train: &common
        split_seed: 57
        num_samples:
          - 32  # num of train images
          - 32  # num of validation images
    validation:
        <<: *common   # reinsert 'train' block content
