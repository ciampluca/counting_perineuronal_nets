# @package _global_

defaults:
 - vgg-cells/detection/common

model:
    module:
        nms: 0.4

data:
    train: &common
        root: "/mnt/datino/VGG_cells"
        split_seed: 87
        num_samples:
          - 8  # num of train images
          - 92  # num of validation images
    validation:
        <<: *common   # reinsert 'train' block content
