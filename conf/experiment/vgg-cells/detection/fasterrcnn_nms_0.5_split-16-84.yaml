# @package _global_

defaults:
 - vgg-cells/detection/common

model:
    module:
        nms: 0.5

data:
    train: &common
        split_seed: 87
        num_samples:
          - 16  # num of train images
          - 84  # num of validation images
    validation:
        <<: *common   # reinsert 'train' block content
