# @package _global_

defaults:
 - bcd-cells/detection/common

seed: 14

data:
    train: &common
        target_params:
            side: 32  
    validation:
        <<: *common   # reinsert 'train' block content
