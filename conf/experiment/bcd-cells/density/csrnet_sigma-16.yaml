# @package _global_

defaults:
 - bcd-cells/density/common

data:
    train: &common
        target_params:
            k_size: 91  
            sigma: 16 
            method: reflect
    validation:
        <<: *common
