# @package _global_

defaults:
 - nuclei-cells/density/common

data:
    train: &common
        split_seed: 57
        num_samples:
            - 8   # num of train images
            - 8  # num of validation images
        target_params:
            method: reflect
    validation:
        <<: *common
