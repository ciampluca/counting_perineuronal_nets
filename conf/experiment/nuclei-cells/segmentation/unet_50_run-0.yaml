# @package _global_

defaults:
 - nuclei-cells/segmentation/common

data:
    train: &common
        split_seed: 87
        num_samples:
            - 50  # num of train images
            - 50  # num of validation images
        max_num_train_val_sample: 100
    validation:
        <<: *common
