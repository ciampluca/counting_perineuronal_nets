# @package _global_
defaults:
  - override /technique: density_based_unet


density_based_unet:
  dataset:
    training:
      params:
        input_size: [640, 640]
    validation:
      params:
        input_size: [640, 640]
        patches_overlap: 120

  training:
    batch_size: 4
