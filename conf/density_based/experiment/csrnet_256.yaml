# @package _global_
defaults:
  - override /technique: density_based_csrnet


density_based_csrnet:
  dataset:
    train:
      params:
        patch_size: 256     # must be divisible by 32
    validation:
      params:
        patch_size: 256     # must be divisible by 32

  optim:
    batch_size: 10
    val_batch_size: 10
    batch_accumulation: 4