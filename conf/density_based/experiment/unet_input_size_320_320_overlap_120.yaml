# @package _global_
defaults:
  - override /technique: density_based_unet


density_based_unet:
  dataset:
    training:
      params:
        input_size: [320, 320]
    validation:
      params:
        input_size: [320, 320]
        patches_overlap: 120

  training:
    batch_size: 6
