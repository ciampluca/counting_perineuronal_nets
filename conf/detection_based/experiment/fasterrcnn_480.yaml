# @package _global_
defaults:
  - override /technique: detection_based_fasterrcnn


detection_based_fasterrcnn:
  dataset:
    train:
      params:
        patch_size: 480
    validation:
      params:
        patch_size: 480
        overlap: 120

  optim:
    batch_size: 12
    val_batch_size: 12
    batch_accumulation: 4

  model:
    params:
      nms: 0.3
