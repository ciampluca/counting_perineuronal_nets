# @package _global_
defaults:
  - override /technique: segmentation_based_unet

segmentation_based_unet:
  dataset:
    train:
      params:
        patch_size: 480

    validation:
      params:
        patch_size: 480
      
  optim:
    batch_size: 4
    batch_accumulation: 2


