# @package _global_
defaults:
  - override /technique: segmentation_based_unet

segmentation_based_unet:
  dataset:
    train:
      params:
        patch_size: 800

    validation:
      params:
        patch_size: 800
      
  optim:
    batch_size: 1
    batch_accumulation: 8


