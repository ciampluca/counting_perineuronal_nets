# @package _global_
defaults:
  - override /technique: segmentation_based_unet

segmentation_based_unet:
  dataset:
    train:
      params:
        patch_size: 320

    validation:
      params:
        patch_size: 320
      
  optim:
    batch_size: 8


