# @package data

train: &common
    target_: detection
    target_params:
        side: 12  # side (in px) of the bounding box localizing a cell

validation:
    <<: *common   # reinsert 'train' block content
