# @package data

train: &common
    target_: density
    target_params:
        k_size: 81  # size (in px) of the kernel of the gaussian localizing a biological structure
        sigma: 13  # sigma of the gaussian
        method: reflect     # method for gt den map generation; can be move, move-cv2, reflect, normalize

validation:
    <<: *common   # reinsert 'train' block content