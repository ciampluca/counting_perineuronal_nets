# @package data

train: &common
    target: detection
    target_params:
        side: 20  # side (in px) of the bounding box localizing a biological structure

validation:
    <<: *common   # reinsert 'train' block content
