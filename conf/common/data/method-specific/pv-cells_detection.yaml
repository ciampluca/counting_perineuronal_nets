# @package data

train: &common
    target: detection
    target_params:
        side: 60       # side (in px) of the bounding box localizing a cell

validation:
    overlap: 120
    <<: *common   # reinsert 'train' block content