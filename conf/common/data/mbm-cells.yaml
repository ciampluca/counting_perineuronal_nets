# @package data

train: &common
    _target_: datasets.CellsDataset
    root: ${hydra:runtime.cwd}/data/mbm-cells
    split: train
    split_seed: 87
    num_samples:
        - 50  # num of train images
        - 50  # num of validation images
    max_num_train_val_sample: 34

validation:
    split: validation
    <<: *common   # reinsert 'train' block content