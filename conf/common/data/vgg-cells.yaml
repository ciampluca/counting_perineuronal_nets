# @package data

train: &common
    _target_: datasets.VGGCellsDataset
    root: ${hydra:runtime.cwd}/data/vgg-cells
    split: train
    split_seed: 87
    num_samples:
        - 64  # num of train images
        - 36  # num of validation images
  
validation:
    split: validation
    <<: *common   # reinsert 'train' block content