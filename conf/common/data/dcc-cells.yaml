# @package data

train: &common
    _target_: datasets.CellsDataset
    root: ${hydra:runtime.cwd}/data/dcc-cells
    split: train
    split_seed: 87
    num_samples:
        - 67  # num of train images - 70% train 30% val
        - 33  # num of validation images - 70% train 30% val
    max_num_train_val_sample: 100
    num_test_samples: 100

validation:
    split: validation
    <<: *common   # reinsert 'train' block content